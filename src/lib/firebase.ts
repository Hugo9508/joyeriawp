'use client';

// Configuración inert para modo DEMO
// No se inicializa Firebase para evitar errores de conexión en el entorno de prueba
const isConfigValid = false;
const db = null;
const storage = null;

export { db, storage, isConfigValid };
